<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Trần Huệ Chi - Lời Mời Trân Trọng</title>
    <link href="https://fonts.googleapis.com/css2?family=Dancing+Script:wght@600&family=Playfair+Display:wght@400;600;700&family=Montserrat:wght@300;400;600&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary: #a67c52;
            --gold: #caa56a;
            --bg: #fdfaf6;
            --text: #4b3b2b;
            --envelope-bg: #f3ede2;
        }

        body {
            margin: 0;
            background: var(--bg);
            font-family: 'Playfair Display', serif;
            color: var(--text);
            overflow-x: hidden;
        }

        /* --- Cover Section (Bao thư) --- */
        .cover {
            height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            background: linear-gradient(rgba(255,255,255,0.3), rgba(255,255,255,0.3)), 
                        url('https://www.transparenttextures.com/patterns/cream-paper.png');
            transition: transform 1s ease;
            text-align: center;
        }

        .envelope-wrapper {
            cursor: pointer;
            position: relative;
            transition: 0.3s;
        }
        .envelope-wrapper:hover { transform: scale(1.05); }

        .envelope {
            width: 280px;
            height: 180px;
            background: var(--envelope-bg);
            border-radius: 4px;
            box-shadow: 0 15px 35px rgba(0,0,0,0.1);
            position: relative;
            display: flex;
            justify-content: center;
            align-items: center;
            overflow: hidden;
        }
        .envelope::before {
            content: ""; position: absolute; top: 0; z-index: 2;
            border-top: 100px solid #e9e0d1; border-left: 140px solid transparent; border-right: 140px solid transparent;
        }
        .envelope::after {
            content: ""; position: absolute; bottom: 0; z-index: 1;
            border-bottom: 100px solid var(--envelope-bg); border-left: 140px solid #ebe2d3; border-right: 140px solid #ebe2d3;
        }
        .wax-seal {
            position: absolute; z-index: 3; width: 50px; height: 50px;
            background: radial-gradient(circle, #e6c17a 0%, #caa56a 100%);
            border-radius: 50%; transform: translateY(-10px);
            display: flex; justify-content: center; align-items: center;
            color: white; font-family: 'Dancing Script'; font-size: 24px; box-shadow: 0 4px 10px rgba(0,0,0,0.2);
        }

        /* --- Main Card --- */
        .container {
            max-width: 800px;
            margin: 40px auto;
            background: white;
            box-shadow: 0 20px 50px rgba(0,0,0,0.05);
            display: none;
            border-radius: 4px;
            opacity: 0;
            transform: translateY(30px);
            transition: all 1s ease;
            position: relative;
            padding: 40px;
            overflow: hidden;
        }
        .container.active { display: block; opacity: 1; transform: translateY(0); }

        /* Header Thiệp */
        .card-header {
            text-align: center;
            margin-bottom: 40px;
            padding-bottom: 20px;
            border-bottom: 1px solid #f0e6d6;
            position: relative;
        }
        .card-header::after {
            content: "✿";
            position: absolute;
            bottom: -12px;
            left: 50%;
            transform: translateX(-50%);
            background: white;
            padding: 0 10px;
            color: var(--gold);
        }
        .header-title {
            font-family: 'Dancing Script', cursive;
            font-size: 50px;
            color: var(--gold);
            margin: 0;
            line-height: 1.2;
        }
        .header-subtitle {
            font-family: 'Dancing Script', cursive;
            font-size: 35px;
            color: var(--gold);
            margin-top: -10px;
        }

        /* Profile Section (Flex) */
        .profile-section {
            display: flex;
            align-items: center;
            gap: 30px;
            margin-bottom: 30px;
            text-align: left;
        }
        .avatar {
            flex-shrink: 0;
            width: 200px;
            height: 200px;
            border-radius: 50%;
            border: 1px solid var(--gold);
            padding: 8px;
            background: white;
            box-shadow: 0 10px 20px rgba(0,0,0,0.05);
        }
        .avatar-inner {
            width: 100%; height: 100%; border-radius: 50%;
            background: url("https://i.postimg.cc/CLKvkLvN/493688393-2376220032747755-4058965580282625632-n.jpg") center/cover;
        }
        .profile-info h2 {
            font-family: 'Dancing Script', cursive;
            font-size: 28px;
            color: #888;
            margin: 0;
        }
        .profile-info .main-name {
            font-size: 40px;
            font-weight: 700;
            color: var(--text);
            margin: 5px 0;
            letter-spacing: 2px;
            text-transform: uppercase;
        }
        .tag-school {
            display: inline-block;
            padding: 5px 15px;
            border: 1px solid var(--gold);
            border-radius: 20px;
            font-size: 14px;
            color: var(--gold);
            font-family: 'Montserrat';
        }
        .quote {
            font-style: italic;
            font-size: 15px;
            color: #666;
            margin-top: 15px;
            line-height: 1.6;
            max-width: 400px;
        }

        /* Detail Section */
        .detail-label {
            background: var(--gold);
            color: white;
            display: inline-block;
            padding: 8px 25px;
            font-family: 'Montserrat';
            font-weight: 600;
            font-size: 13px;
            letter-spacing: 2px;
            margin: 30px 0 20px;
            text-transform: uppercase;
        }

        .info-box {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
            text-align: center;
        }
        .info {
            padding: 30px;
            background: #fffcf9;
            border: 1px solid #f0e6d6;
        }
        .info h3 {
            font-size: 14px;
            color: var(--gold);
            text-transform: uppercase;
            letter-spacing: 2px;
            margin-bottom: 10px;
        }

        .countdown {
            padding: 20px 0;
            font-family: 'Montserrat';
            font-size: 16px;
            border-top: 1px solid #eee;
            margin: 20px 0;
            letter-spacing: 1px;
            color: #888;
        }

        /* RSVP */
        .rsvp-section { margin-top: 50px; }
        .status-buttons { display: flex; gap: 10px; margin: 15px 0; }
        .status-btn {
            flex: 1; padding: 12px; border: 1px solid #ddd; background: white;
            cursor: pointer; font-family: 'Montserrat'; font-size: 12px;
        }
        .status-btn.selected { background: var(--gold); color: white; border-color: var(--gold); }
        .submit-btn {
            background: var(--text); color: white; width: 100%; padding: 15px;
            border: none; margin-top: 20px; cursor: pointer; letter-spacing: 2px;
        }

        @media (max-width: 768px) {
            .profile-section { flex-direction: column; text-align: center; }
            .info-box { grid-template-columns: 1fr; }
            .container { padding: 20px; margin: 10px; }
            .header-title { font-size: 40px; }
        }
    </style>
</head>
<body>

<div class="cover" id="cover">
    <div style="font-family: 'Montserrat'; letter-spacing: 4px; color: #888; margin-bottom: 10px;">THƯ MỜI DỰ LỄ TỐT NGHIỆP</div>
    <div style="font-family: 'Dancing Script'; font-size: 35px; color: var(--gold); margin-bottom: 30px;">Trần Huệ Chi</div>
    <div class="envelope-wrapper" onclick="openCard()">
        <div class="envelope">
            <div class="wax-seal">C</div>
        </div>
    </div>
    <div style="margin-top: 20px; font-family: 'Montserrat'; font-size: 12px; color: #aaa; letter-spacing: 2px;">CHẠM ĐỂ MỞ THƯ</div>
</div>

<div class="container" id="card">
    <!-- Phần Header Thiệp Mới Thêm -->
    <div class="card-header">
        <h1 class="header-title">Thiệp Mời</h1>
        <div class="header-subtitle">Tốt Nghiệp</div>
    </div>

    <!-- Phần Giới Thiệu (Avatar bên trái, Text bên phải) -->
    <div class="profile-section">
        <div class="avatar"><div class="avatar-inner"></div></div>
        <div class="profile-info">
            <h2>Mời bạn đến tham dự cùng</h2>
            <div class="main-name">Trần Huệ Chi</div>
            <div class="tag-school">Đại học Thăng Long</div>
            <p class="quote">"Thanh xuân như một cơn mưa rào. Hãy cùng mình lưu giữ những khoảnh khắc rực rỡ nhất của tuổi học trò trước khi chúng ta mỗi người một ngả."</p>
        </div>
    </div>

    <div style="text-align: center;">
        <div class="countdown" id="countdown">Đang đếm ngược...</div>
        
        <div class="detail-label">Thông tin chi tiết</div>
        
        <div class="info-box">
            <div class="info">
                <h3>Thời gian</h3>
                <p><strong>08:00 AM</strong></p>
                <p>Thứ 7, 11/04/2026</p>
            </div>
            <div class="info">
                <h3>Địa điểm</h3>
                <p><strong>Đại học Thăng Long</strong></p>
                <p style="font-size: 14px;">Nghiêm Xuân Yêm, Đại Kim, Hoàng Mai, Hà Nội</p>
            </div>
        </div>
    </div>

    <!-- RSVP -->
    <div class="rsvp-section">
        <h2 style="text-align:center; font-family: 'Dancing Script'; font-size: 35px; color: var(--gold);">Xác nhận tham dự</h2>
        <input type="text" id="name" placeholder="Tên của bạn..." style="width:100%; padding:12px; margin-bottom:10px; border:1px solid #eee; box-sizing: border-box;">
        <textarea id="wish" placeholder="Lời chúc của bạn..." rows="2" style="width:100%; padding:12px; border:1px solid #eee; box-sizing: border-box;"></textarea>
        
        <div class="status-buttons">
            <button class="status-btn" onclick="selectStatus(this, 'Tham dự')">SẼ THAM DỰ</button>
            <button class="status-btn" onclick="selectStatus(this, 'Tiếc quá')">XIN VẮNG MẶT</button>
        </div>
        <button class="submit-btn" onclick="sendMessage()">GỬI LỜI XÁC NHẬN</button>
    </div>

    <div id="messages" style="margin-top: 30px;"></div>
</div>

<audio id="music" loop>
    <source src="https://www.soundhelix.com/examples/mp3/SoundHelix-Song-1.mp3" type="audio/mpeg">
</audio>

<script>
function openCard(){
    document.getElementById("cover").style.opacity = "0";
    setTimeout(() => {
        document.getElementById("cover").style.display = "none";
        const card = document.getElementById("card");
        card.style.display = "block";
        setTimeout(() => card.classList.add('active'), 50);
    }, 800);
    document.getElementById("music").play().catch(() => {});
}

const targetDate = new Date(2026, 3, 11, 8, 0, 0).getTime();
setInterval(() => {
    const diff = targetDate - new Date().getTime();
    if (diff < 0) { document.getElementById("countdown").innerHTML = "Sự kiện đã diễn ra"; return; }
    const d = Math.floor(diff / 86400000);
    const h = Math.floor((diff % 86400000) / 3600000);
    const m = Math.floor((diff % 3600000) / 60000);
    const s = Math.floor((diff % 60000) / 1000);
    document.getElementById("countdown").innerHTML = `${d} ngày : ${h} giờ : ${m} phút : ${s} giây`;
}, 1000);

let status = "";
function selectStatus(btn, s) {
    document.querySelectorAll('.status-btn').forEach(b => b.classList.remove('selected'));
    btn.classList.add('selected');
    status = s;
}

// Biến toàn cục để lưu trạng thái tham dự
let selectedStatus = ""; 

function selectStatus(btn, s) {
    // Xóa class selected ở tất cả các nút
    document.querySelectorAll('.status-btn').forEach(b => b.classList.remove('selected'));
    // Thêm class cho nút vừa bấm
    btn.classList.add('selected');
    // Gán giá trị vào biến selectedStatus
    selectedStatus = s; 
}

async function sendMessage() {
    const n = document.getElementById("name").value;
    const w = document.getElementById("wish").value;
    
    // Kiểm tra tên
    if (!n) {
        alert("Bạn vui lòng nhập tên nhé!");
        return;
    }
    // Kiểm tra trạng thái (Sử dụng biến selectedStatus)
    if (!selectedStatus) {
        alert("Bạn vui lòng chọn trạng thái tham dự nhé!");
        return;
    }

    const btn = document.querySelector('.submit-btn');
    const originalText = btn.innerText;
    btn.innerText = "ĐANG GỬI...";
    btn.disabled = true;

    const data = { name: n, status: selectedStatus, wish: w };

    try {
        // Link Apps Script của bạn
        const scriptURL = 'https://script.google.com/macros/s/AKfycbx5_njjpTeNWAZZ8vsL7nq5R1twm4hZURvIapAtnuSk-zlcFQTeF_tO9ly7EAKbScjSMg/exec';

        await fetch(scriptURL, {
            method: 'POST',
            mode: 'no-cors',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify(data)
        });

        // Hiển thị ngay lên màn hình
        const m = document.createElement("div");
        m.className = "message-item";
        m.innerHTML = `
            <span class="status-tag">${selectedStatus}</span>
            <strong>${n}</strong>
            <p>"${w || 'Chúc bạn có một buổi lễ thật ý nghĩa!'}"</p>
        `;
        document.getElementById("messages").prepend(m);

        // Reset lại form
        document.getElementById("name").value = "";
        document.getElementById("wish").value = "";
        document.querySelectorAll('.status-btn').forEach(b => b.classList.remove('selected'));
        selectedStatus = ""; // Reset biến
        
        alert("Cảm ơn bạn! Lời xác nhận đã được gửi đi.");

    } catch (error) {
        console.error('Lỗi:', error);
        alert("Có lỗi kết nối, nhưng lời chúc đã được hiển thị tạm thời trên trang.");
    } finally {
        btn.innerText = originalText;
        btn.disabled = false;
    }
}
</script>

</body>
</html>
